name: Build and Release

on:
  release:
    types: [created]
  workflow_dispatch:  # Allows manual trigger

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pandas requests

      - name: Build executable
        run: |
          pyinstaller --onefile --windowed --name "iNaturalist-Downloader" --icon=NONE inaturalist_downloader.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-executable
          path: dist/iNaturalist-Downloader.exe

  build-macos-intel:
    runs-on: macos-13  # Intel Mac
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pandas requests

      - name: Build executable
        run: |
          pyinstaller --onefile --windowed --name "iNaturalist-Downloader" inaturalist_downloader.py

      - name: Create ZIP for macOS Intel
        run: |
          cd dist
          zip -r iNaturalist-Downloader-macOS-Intel.zip iNaturalist-Downloader.app || zip -r iNaturalist-Downloader-macOS-Intel.zip iNaturalist-Downloader

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-intel-executable
          path: dist/iNaturalist-Downloader-macOS-Intel.zip

  build-macos-apple-silicon:
    runs-on: macos-latest  # Apple Silicon (M1/M2/M3)
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pandas requests

      - name: Build executable
        run: |
          pyinstaller --onefile --windowed --name "iNaturalist-Downloader" inaturalist_downloader.py

      - name: Create ZIP for macOS Apple Silicon
        run: |
          cd dist
          zip -r iNaturalist-Downloader-macOS-AppleSilicon.zip iNaturalist-Downloader.app || zip -r iNaturalist-Downloader-macOS-AppleSilicon.zip iNaturalist-Downloader

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-apple-silicon-executable
          path: dist/iNaturalist-Downloader-macOS-AppleSilicon.zip

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pandas requests

      - name: Build executable
        run: |
          pyinstaller --onefile --name "iNaturalist-Downloader" inaturalist_downloader.py

      - name: Make executable and create archive
        run: |
          chmod +x dist/iNaturalist-Downloader
          cd dist
          tar -czvf iNaturalist-Downloader-Linux.tar.gz iNaturalist-Downloader

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-executable
          path: dist/iNaturalist-Downloader-Linux.tar.gz

  upload-release:
    needs: [build-windows, build-macos-intel, build-macos-apple-silicon, build-linux]
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure
        run: ls -R artifacts

      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/windows-executable/iNaturalist-Downloader.exe
            artifacts/macos-intel-executable/iNaturalist-Downloader-macOS-Intel.zip
            artifacts/macos-apple-silicon-executable/iNaturalist-Downloader-macOS-AppleSilicon.zip
            artifacts/linux-executable/iNaturalist-Downloader-Linux.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
